# Windows repair: avoid isolated symlink layout (EPERM during pnpm install).
# package-import-method=copy avoids hardlink/symlink import from the store.
# Does not change application source; only package linking strategy.
node-linker=hoisted
package-import-method=copy
