# Landing page API origin (no trailing slash, no /api suffix).
# Production builds default to https://api.skillad.in when unset.
# Dev leaves this empty so requests use relative /api via the Vite proxy.
VITE_API_BASE=https://api.skillad.in
